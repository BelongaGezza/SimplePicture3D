# Depth estimator CLI (Sprint 1.3). Python 3.10+
# Minimal for stub mode (roundtrip tests without model):
Pillow>=10.0

# For real Depth-Anything-V2 inference (AI-204, AI-205):
# Install PyTorch first per https://pytorch.org/get-started/locally/
#   CPU: pip install torch torchvision
#   CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   macOS Metal: pip install torch torchvision
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
